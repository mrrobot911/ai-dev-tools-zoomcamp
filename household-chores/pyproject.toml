[project]
name = "household-chores"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kelwin", email = "kelwin90210@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "django>=4.2.0",
    "pytest>=9.1.1",
]

[project.scripts]
household-chores = "household_chores:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
